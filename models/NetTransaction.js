
class NetTransaction {
    constructor(transaction, netPercentage, netDollar) {
        this.transaction = transaction;
        this.netPercentage = netPercentage;
        this.netDollar = netDollar;
    }
}

module.exports = NetTransaction;